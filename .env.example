NEXT_PUBLIC_API_URL=http://localhost:5000
# Production (required for mobile auth): your live Vercel URL — enables
# same-origin /api/auth + /api/gateway proxies so cookies work on Safari.
# NEXT_PUBLIC_SITE_URL=https://your-app.vercel.app
